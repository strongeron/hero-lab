node_modules
dist
dist-ssr
*.local
.DS_Store
.vite
node_modules/.tmp

# Editor
.vscode/*
!.vscode/extensions.json
.idea

# Local QA output (dogfood runs) — screenshots, reports; not repo content
dogfood-output/

# Rejected OG card alternates — kept on disk for reference, not repo content.
# The chosen card ships as public/og-image.jpg.
docs/og-candidates/

# Local Netlify folder
.netlify

# Cover variants are regenerable output of docs/cover/generate-cover.py.
# Only the shipped SVG is tracked.
docs/cover/cover-a1-*.svg
